# Installa con: pip install -r requirements.txt
# Su SMCE (JupyterHub): pip install -r requirements.txt --user

numpy
scipy
pandas
xarray
netCDF4
zarr
rasterio
pyproj
tqdm
ipywidgets
# torch viene di solito pre-installato su JupyterHub NASA SMCE
# se non presente: pip install torch torchvision
